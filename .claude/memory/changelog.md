# Changelog (engineering notes) — Project Bootstrapper

User-facing releases live in the root `CHANGELOG.md`. This file is for engineering notes between releases.

- 0.1.0 — initial engine + engine/ (engine), engine/src/cli.ts (CLI), mcp-server/ (MCP server), web/ (Next.js app); tests + CI + security + .claude workspace.
