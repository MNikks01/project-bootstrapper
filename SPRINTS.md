# Project Bootstrapper — SPRINTS

~3–4 weeks to MVP. Cheapest project; largely falls out of building the lab's own repos. Build opportunistically.

- **S0 (Wk0):** CLI scaffold; template engine.
- **S1 (Wk1–2):** AI-SaaS template + agent files + CI/Docker/observability/guardrails/eval skeleton; template build-test gate; download zip.
- **S2 (Wk3):** GitHub push; CLI polish; OSS the CLI + templates. **Exit:** one command → production-ready, AI-ready repo on GitHub. **Public launch (low-key).**

### → MVP COMPLETE (~3 weeks).

## V1 (Wk4+, opportunistic)
Web wizard + feature toggles + NL-brief customization + teams/saved configs.
## V2
Org templates + golden-path + **ContextOS integration** (its real home).
## V3
Enterprise standards/registry (only if pulled).

**Cadence:** demoable per sprint; templates always build (gate). Don't over-invest — graduate effort into ContextOS as a feature. The lab's own 7 repos are the first templates.
