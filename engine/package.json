{
  "name": "@mnikks01/bootstrap",
  "version": "0.2.0",
  "type": "module",
  "description": "Project Bootstrapper (#5) engine — turn a spec (template + feature toggles) into a complete, production-ready, AI-agent-ready project (code + CLAUDE.md/AGENTS.md/mcp.json + CI + Docker) as a file map. Node native TypeScript; zero-network.",
  "bin": {
    "bootstrap": "dist/cli.js"
  },
  "scripts": {
    "demo": "node scripts/demo.ts",
    "test": "node --test test/*.test.ts",
    "cli": "node src/cli.ts",
    "build": "npx --yes esbuild@0.24.2 src/cli.ts --bundle --platform=node --format=esm --target=node18 --packages=external --outfile=dist/cli.js",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MNikks01/project-bootstrapper.git",
    "directory": "engine"
  },
  "homepage": "https://github.com/MNikks01/project-bootstrapper#readme",
  "bugs": "https://github.com/MNikks01/project-bootstrapper/issues",
  "publishConfig": {
    "access": "public"
  }
}
